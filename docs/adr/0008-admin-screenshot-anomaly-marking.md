# 管理员可维护截图异常标记

管理员可以在当前已发布批次的视频行操作中维护四个截图字段的异常标记。保存时只增删这些截图字段在现有异常标记中的键，保留 Excel 导入或其它来源产生的异常标记；即使截图尚未上传，也可先标记为异常以表示需要补充。历史批次继续只读。该决策 supersede 了 [ADR 0002](./0002-excel-red-highlight-anomalies.md) 中“仅导入识别、不支持手动切换标红状态”的限制，同时继续复用同一套异常统计、展示和导出语义。每次确认按一次管理员操作写入审计日志，记录新增和取消的截图异常字段。
